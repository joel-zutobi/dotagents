interface:
  display_name: "Orchestrate"
  short_description: "Choose efficient lanes, reviews, and verification"
  default_prompt: "Use $orchestrate to run this work with an appropriate mode, persistent coding lanes, proportional reviews, and reusable checks."
policy:
  allow_implicit_invocation: true
